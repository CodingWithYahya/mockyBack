server:
  port: 8084

spring:
  application:
    name: mockyBackend
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB



logging:
  level:
    org:
      springframework: DEBUG